#!/usr/bin/env node

var codex = require('../'),
    program = require('commander'),
    color = require('color'),
    mkdirp = require('mkdirp');
    

program
  .version(codex.version);

program.parse(process.argv);